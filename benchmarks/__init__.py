"""paretobandits benchmark suite.

Each YAML in `benchmarks/configs/` defines one canonical setup. Running
`python -m benchmarks.run --config benchmarks/configs/<name>.yaml`
reproduces the numbers in `BENCHMARK.md`.
"""
